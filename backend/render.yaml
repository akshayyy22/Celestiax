services:
  - type: web
    name: celestiax-backend
    env: rust
    buildCommand: "cargo build --release"
    startCommand: "cargo run --release"
    plan: free
    envVars:
      - key: PORT
        value: "10000" # Optional; Render defaults to 10000
